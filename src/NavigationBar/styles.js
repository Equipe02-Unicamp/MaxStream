import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  footerArea: {
    flex: .1,
    backgroundColor: '#c7f9',
    alignItems: 'center',
    justifyContent: 'center',
  }
});